const sendEmail = async ({ to, subject, text }) => {
    console.log(`📧 Sending email to ${to} | Subject: ${subject}`);
    // You can implement real mail logic here later
  };
  
  module.exports = sendEmail;
  